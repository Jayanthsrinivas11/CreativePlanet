def pay(h,r):
    if hours>40:
        print(float(hours-40)*float(1.5)*float(rate)+40*float(rate))
    else:
        print(float(hours)*float(rate)
    return = pay


hours = input('please enter the number of hours you worked?')
rate = input('please enter the rate of pay per hour?')
print(pay)

   
